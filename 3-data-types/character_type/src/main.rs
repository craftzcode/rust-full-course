//! Character Type
// Its represents a single Unicode character
// Unicode is a computing standard for the representation of text for most of the world's writing systems
// A Unicode character doesn't necessarily have to be an alphabetic character
// For example, the Unicode standard also supports emojis, various symbols, and more

fn main() {
    //* Char
    // A character is always going to be marked with single quotes (''), and it can only be one character
    let middle_initial: char 'M'; 
    let emoji: char = '🦀';
}
