//! Visual Separator for Numbers
// We can use the (_) as visual separators for numbers, making them easier to read
// We use (_) to separate groups of 3 digits

fn main() {
    let sixteen_bit_signed: i16 = 32_500;
    let sixteen_bit_unsigned: u32 = 6_132_500;
}
